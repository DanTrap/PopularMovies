<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Movies" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/starfleet_blue</item>
        <item name="colorPrimaryVariant">@color/calcareous_sinter</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/calcareous_sinter</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
    </style>
    
    <style name="TextAppearance.Material3.ActionBar.Title.DanTrap">
        <item name="android:textSize">25sp</item>
    </style>

    <style name="rounded_imageview" parent="">
        <item name="cornerSize">5dp</item>
    </style>
</resources>